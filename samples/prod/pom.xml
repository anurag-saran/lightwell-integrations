<?xml version="1.0" encoding="UTF-8"?>
<!--
  Production. Run ./scripts/setup-prod.sh first.
  The token stays on Artifactory or Nexus. Do not put it in this file.
  lightwell-java searches predisclosure, then remediated, then validated.

  Default URL is the local Artifactory virtual repository.
  Nexus: mvn -f samples/prod/pom.xml dependency:resolve \
    -Dlightwell.repo.url=http://127.0.0.1:8083/repository/lightwell-java

  Change lightwell.version to the production .rhlw build you are adopting.
  The version below is also on the public demo, so this file can be tried
  before a production token is configured.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.example</groupId>
  <artifactId>lightwell-prod</artifactId>
  <version>1.0.0</version>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <lightwell.repo.url>http://127.0.0.1:8082/artifactory/lightwell-java</lightwell.repo.url>
    <lightwell.version>1.33.0.rhlw-00001</lightwell.version>
  </properties>

  <repositories>
    <repository>
      <id>lightwell-java</id>
      <name>Local Lightwell Java</name>
      <url>${lightwell.repo.url}</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>org.yaml</groupId>
      <artifactId>snakeyaml</artifactId>
      <version>${lightwell.version}</version>
    </dependency>
  </dependencies>
</project>
